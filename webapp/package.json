{
  "name": "asset-tracker-web",
  "version": "1.0.0",
  "private": true,
  "description": "Web dashboard for AssetTracker",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
